# ═══════════════════════════════════════════════════════════════════════════════
# QANTUM Framework - Kubernetes Secrets Template
# ⚠️  IMPORTANT: DO NOT COMMIT ACTUAL SECRETS TO VERSION CONTROL
# ═══════════════════════════════════════════════════════════════════════════════
# 
# Instructions:
# 1. Copy this file: cp secrets.yml.template secrets.yml
# 2. Replace all REPLACE_WITH_* placeholders with actual base64-encoded values
# 3. Encode secrets: echo -n "your-secret" | base64
# 4. Apply: kubectl apply -f k8s/secrets.yml
# 5. Add secrets.yml to .gitignore to prevent accidental commits
# 
# ═══════════════════════════════════════════════════════════════════════════════

apiVersion: v1
kind: Secret
metadata:
  name: qantum-secrets
  labels:
    app: qantum-framework
type: Opaque
data:
  # ─────────────────────────────────────────────────────────────────────────────
  # Database Credentials
  # ─────────────────────────────────────────────────────────────────────────────
  # Example: echo -n "postgresql://qantum_user:password@postgres:5432/qantum_db" | base64
  database-url: REPLACE_WITH_BASE64_DATABASE_URL
  
  # Example: echo -n "super_secure_password_123" | base64
  postgres-password: REPLACE_WITH_BASE64_POSTGRES_PASSWORD
  
  # ─────────────────────────────────────────────────────────────────────────────
  # Payment Gateway (Stripe)
  # ─────────────────────────────────────────────────────────────────────────────
  # Example: echo -n "sk_live_..." | base64
  stripe-secret-key: REPLACE_WITH_BASE64_STRIPE_SECRET_KEY
  
  # Example: echo -n "whsec_..." | base64
  stripe-webhook-secret: REPLACE_WITH_BASE64_STRIPE_WEBHOOK_SECRET
  
  # ─────────────────────────────────────────────────────────────────────────────
  # Liveness Token Security
  # ─────────────────────────────────────────────────────────────────────────────
  # Generate with: openssl rand -hex 32 | base64
  liveness-token-secret: REPLACE_WITH_BASE64_LIVENESS_TOKEN_SECRET
  
  # ─────────────────────────────────────────────────────────────────────────────
  # Admin Keys
  # ─────────────────────────────────────────────────────────────────────────────
  # Example: echo -n "ed25519_public_key_here" | base64
  admin-pub-key: REPLACE_WITH_BASE64_ADMIN_PUB_KEY
  
  # ─────────────────────────────────────────────────────────────────────────────
  # Message Queue
  # ─────────────────────────────────────────────────────────────────────────────
  # Example: echo -n "rabbitmq_password_123" | base64
  rabbitmq-password: REPLACE_WITH_BASE64_RABBITMQ_PASSWORD
  
  # ─────────────────────────────────────────────────────────────────────────────
  # Monitoring
  # ─────────────────────────────────────────────────────────────────────────────
  # Example: echo -n "grafana_admin_password" | base64
  grafana-password: REPLACE_WITH_BASE64_GRAFANA_PASSWORD

---
# ═══════════════════════════════════════════════════════════════════════════════
# TLS Certificate Secret (managed by cert-manager)
# ═══════════════════════════════════════════════════════════════════════════════
# 
# This will be automatically created by cert-manager using Let's Encrypt.
# If you want to use custom certificates, uncomment and fill in:
# 
# apiVersion: v1
# kind: Secret
# metadata:
#   name: qantum-tls-secret
#   namespace: default
# type: kubernetes.io/tls
# data:
#   tls.crt: REPLACE_WITH_BASE64_TLS_CERTIFICATE
#   tls.key: REPLACE_WITH_BASE64_TLS_PRIVATE_KEY

---
# ═══════════════════════════════════════════════════════════════════════════════
# Monitoring Basic Auth Secret
# ═══════════════════════════════════════════════════════════════════════════════
# 
# Generate with: htpasswd -c auth admin
# Then: cat auth | base64
# 
apiVersion: v1
kind: Secret
metadata:
  name: monitoring-basic-auth
  labels:
    app: qantum-monitoring
type: Opaque
data:
  # Example: echo "admin:$(openssl passwd -apr1 your_password)" | base64
  auth: REPLACE_WITH_BASE64_HTPASSWD_AUTH

# ═══════════════════════════════════════════════════════════════════════════════
# 🔐 SECURITY NOTES:
# ═══════════════════════════════════════════════════════════════════════════════
# 
# 1. Use strong, randomly generated passwords for all credentials
# 2. Rotate secrets regularly (recommended: every 90 days)
# 3. Use Kubernetes RBAC to restrict access to secrets
# 4. Consider using external secret management (HashiCorp Vault, AWS Secrets Manager)
# 5. Enable encryption at rest for etcd
# 6. Use network policies to restrict pod communication
# 7. Implement Pod Security Standards (PSS)
# 
# Generate secure passwords:
#   openssl rand -hex 32
#   pwgen -s 64 1
# 
# Together with logic and security, we are invincible.
# ═══════════════════════════════════════════════════════════════════════════════
